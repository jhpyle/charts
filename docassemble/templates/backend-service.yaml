apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-docassemble-backend-service
spec:
  selector:
    app: {{ .Release.Name }}-docassemble-backend
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
